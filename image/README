To run this tutorial you need to download the zip file from
https://storage.googleapis.com/download.tensorflow.org/models/inception5h.zip

And extract the file 'tensorflow_inception_graph.pb' in this directory.

Then you can simply do
ruby classify_image.rb <image_file>

NOTE: If <image_file> is not provided it will use the test image 'mysore_palace.jpg'
